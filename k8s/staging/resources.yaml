---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: identidade-carioca-authz
  labels:
    app: identidade-carioca-authz
  annotations:
    secrets.infisical.com/auto-reload: "true"
spec:
  selector:
    matchLabels:
      app: identidade-carioca-authz
  template:
    metadata:
      labels:
        app: identidade-carioca-authz
    spec:
      containers:
        - name: authz
          image: ghcr.io/prefeitura-rio/identidade-carioca-authz:latest
          ports:
            - containerPort: 8000
              name: http
              protocol: TCP
            - containerPort: 9000
              name: grpc
              protocol: TCP
          envFrom:
            - secretRef:
                name: cerbos-authz-secrets
          resources:
            requests:
              memory: "256Mi"
              cpu: "500m"
            limits:
              memory: "512Mi"
              cpu: "2000m"
          securityContext:
            runAsNonRoot: true
            runAsUser: 1001
            runAsGroup: 1001
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
      securityContext:
        fsGroup: 1001
      terminationGracePeriodSeconds: 30

---
apiVersion: v1
kind: Service
metadata:
  name: identidade-carioca-authz
  labels:
    app: identidade-carioca-authz
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
    - port: 9000
      targetPort: 9000
      protocol: TCP
      name: grpc
  selector:
    app: identidade-carioca-authz

---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: identidade-carioca-authz-hpa
  labels:
    app: identidade-carioca-authz
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: identidade-carioca-authz
  minReplicas: 2
  maxReplicas: 25
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 60
      policies:
        - type: Percent
          value: 10
          periodSeconds: 60
    scaleUp:
      stabilizationWindowSeconds: 60
      policies:
        - type: Percent
          value: 50
          periodSeconds: 30
        - type: Pods
          value: 2
          periodSeconds: 30

---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: identidade-carioca-authz-pdb
  labels:
    app: identidade-carioca-authz
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: identidade-carioca-authz
